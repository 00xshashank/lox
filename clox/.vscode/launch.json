{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\clox.exe",
            "args": [], 
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            // "MIMode": "gdb", // Use "gdb" or "lldb" for Linux/macOS. For Windows, use "windows" with MSVC.
            "preLaunchTask": "Pre-Debug Task"
        }
    ]
}
